# Current FATHER OSINT DEV runtime dependencies.
# The approved `father_osint` DEV core currently uses the Python standard library only.
#
# For verification/tests install:
#   python -m pip install -r requirements-dev.txt
#
# Historical Ollama/GPU dependencies were moved to:
#   requirements-legacy.txt
