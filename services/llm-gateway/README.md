# LLM Gateway

**Status:** LEGACY/EXPERIMENTAL.

This service predates the current minimal FATHER OSINT contract. The current DEV Knowledge Factory does not require a networked LLM gateway because Analyst and Socrates are deterministic stubs used only to prove handoffs.

Future activation path:

```text
approved LLM requirement
    ↓
provider / cost / security constraints
    ↓
gateway interface contract
    ↓
acceptance tests
    ↓
donor/technology review
    ↓
PoC
    ↓
implementation decision
```

Until then, preserve the code but do not couple `father_osint` to this service.
